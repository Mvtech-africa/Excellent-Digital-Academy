<!doctype html>
<html lang="en">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width,initial-scale=1" />
  <title>Scrollable & Draggable Testimonials</title>
  <style>
    :root{
      --bg:#0f1724; /* dark background */
      --card:#0b1220;
      --accent: #06b6d4;
      --text:#e6eef6;
      --muted:#98a0aa;
    }
    *{box-sizing:border-box}
    body{margin:0;font-family:Inter,ui-sans-serif,system-ui,-apple-system,Segoe UI,Roboto,'Helvetica Neue',Arial;color:var(--text);background:linear-gradient(180deg,#071027 0%, #071122 100%);padding:40px}

    .wrap{max-width:1100px;margin:0 auto}
    h1{font-size:20px;margin:0 0 18px}

    .track-wrap{position:relative}

    /* Scrollable track */
    .track{
      display:flex;gap:18px;overflow-x:auto;padding:18px;border-radius:12px;scroll-snap-type:x mandatory;-webkit-overflow-scrolling:touch;background:linear-gradient(90deg, rgba(255,255,255,0.02), rgba(255,255,255,0.01));cursor:grab;user-select:none}
    .track:active{cursor:grabbing}
    .track::-webkit-scrollbar{height:10px}
    .track::-webkit-scrollbar-thumb{background:rgba(255,255,255,0.06);border-radius:999px}

    /* testimonial card */
    .card{min-width:300px;max-width:340px;background:linear-gradient(180deg, rgba(255,255,255,0.02), rgba(255,255,255,0.01));border:1px solid rgba(255,255,255,0.04);padding:18px;border-radius:12px;scroll-snap-align:center;flex-shrink:0;display:flex;flex-direction:column;gap:12px}
    .meta{display:flex;gap:12px;align-items:center}
    .meta img{width:48px;height:48px;border-radius:50%;object-fit:cover;border:2px solid rgba(255,255,255,0.04)}
    .name{font-weight:600}
    .role{color:var(--muted);font-size:13px}
    .quote{font-size:15px;line-height:1.45;color:var(--text)}

    /* left & right fade overlays */
    .fade{position:absolute;top:0;bottom:0;width:56px;pointer-events:none}
    .fade.left{left:0;background:linear-gradient(90deg, rgba(7,17,34,0.95) 0%, rgba(7,17,34,0.00) 100%)}
    .fade.right{right:0;background:linear-gradient(270deg, rgba(7,17,34,0.95) 0%, rgba(7,17,34,0.00) 100%)}

    /* arrows (optional) */
    .arrow{position:absolute;top:50%;transform:translateY(-50%);z-index:6;background:rgba(2,6,23,0.6);border:1px solid rgba(255,255,255,0.04);backdrop-filter:blur(6px);width:40px;height:40px;border-radius:10px;display:flex;align-items:center;justify-content:center;cursor:pointer}
    .arrow svg{width:18px;height:18px;opacity:0.9}
    .arrow.left{left:10px}
    .arrow.right{right:10px}

    /* responsive */
    @media (max-width:640px){
      .card{min-width:260px}
      .wrap{padding:0 12px}
    }
  </style>
</head>
<body>
  <div class="wrap">
    <h1>What people say</h1>
    <p style="color:var(--muted);margin:6px 0 14px">Drag (hold + move) or swipe testimonials left/right. Click arrows or use keyboard ← → for quick navigation.</p>

    <div class="track-wrap">
      <button class="arrow left" aria-label="scroll left">
        <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M15 18l-6-6 6-6" stroke-linecap="round" stroke-linejoin="round"></path></svg>
      </button>

      <div class="track" id="testimonials">
        <!-- Repeat cards as needed -->
        <article class="card">
          <div class="meta">
            <img src="https://images.unsplash.com/photo-1544005313-94ddf0286df2?q=80&w=400&auto=format&fit=crop&ixlib=rb-4.0.3&s=1a" alt="Aisha">
            <div>
              <div class="name">Aisha Bello</div>
              <div class="role">Product Designer — Lagos</div>
            </div>
          </div>
          <div class="quote">"This kit levelled up our product visuals in days. The team loved how fast we could iterate and ship."</div>
        </article>

        <article class="card">
          <div class="meta">
            <img src="https://images.unsplash.com/photo-1545996124-1b3a0b5edb5b?q=80&w=400&auto=format&fit=crop&ixlib=rb-4.0.3&s=2b" alt="Chidi">
            <div>
              <div class="name">Chidi Nnaji</div>
              <div class="role">Founder — TechStart</div>
            </div>
          </div>
          <div class="quote">"Customer conversion increased after we added the testimonial section — believable, human, and easy to read."</div>
        </article>

        <article class="card">
          <div class="meta">
            <img src="https://images.unsplash.com/photo-1544725176-7c40e5a2c9f9?q=80&w=400&auto=format&fit=crop&ixlib=rb-4.0.3&s=3c" alt="Maya">
            <div>
              <div class="name">Maya Ade</div>
              <div class="role">Marketing Lead</div>
            </div>
          </div>
          <div class="quote">"Clear layout, easy to navigate on mobile, and the drag interaction is a nice touch for touch-first users."</div>
        </article>

        <article class="card">
          <div class="meta">
            <img src="https://images.unsplash.com/photo-1547425260-76bcadfb4f2c?q=80&w=400&auto=format&fit=crop&ixlib=rb-4.0.3&s=4d" alt="Tunde">
            <div>
              <div class="name">Tunde Okoro</div>
              <div class="role">CTO — FinEdge</div>
            </div>
          </div>
          <div class="quote">"Lightweight and fast — integrates seamlessly into our landing page without blocking other elements."</div>
        </article>

        <article class="card">
          <div class="meta">
            <img src="https://images.unsplash.com/photo-1544006659-f0b21884ce1d?q=80&w=400&auto=format&fit=crop&ixlib=rb-4.0.3&s=5e" alt="Grace">
            <div>
              <div class="name">Grace N.</div>
              <div class="role">Freelance Writer</div>
            </div>
          </div>
          <div class="quote">"I love the card sizing and the smooth scrolling — feels polished and natural on mobile."</div>
        </article>

      </div>

      <button class="arrow right" aria-label="scroll right">
        <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M9 6l6 6-6 6" stroke-linecap="round" stroke-linejoin="round"></path></svg>
      </button>

      <div class="fade left"></div>
      <div class="fade right"></div>
    </div>
  </div>

  <script>
    (function(){
      const track = document.getElementById('testimonials');
      let isDown = false;
      let startX;
      let scrollLeft;
      let velocity = 0;
      let rafId = null;

      // Pointer-based dragging (works for mouse + touch)
      track.addEventListener('pointerdown', (e) => {
        isDown = true;
        track.setPointerCapture(e.pointerId);
        startX = e.clientX;
        scrollLeft = track.scrollLeft;
        velocity = 0;
        track.classList.add('dragging');
        e.preventDefault();
      });

      track.addEventListener('pointermove', (e) => {
        if(!isDown) return;
        const x = e.clientX;
        const walk = (startX - x); // pixels moved
        const prev = track.scrollLeft;
        track.scrollLeft = scrollLeft + walk;
        velocity = track.scrollLeft - prev; // simple velocity
      });

      function stopDrag(e){
        if(!isDown) return;
        isDown = false;
        try{ if(e && e.pointerId) track.releasePointerCapture(e.pointerId); }catch(_){}
        track.classList.remove('dragging');
        // momentum scrolling
        cancelAnimationFrame(rafId);
        const step = () => {
          if(Math.abs(velocity) < 0.5) return;
          track.scrollLeft += velocity;
          velocity *= 0.95; // friction
          rafId = requestAnimationFrame(step);
        };
        rafId = requestAnimationFrame(step);
      }
      track.addEventListener('pointerup', stopDrag);
      track.addEventListener('pointercancel', stopDrag);
      track.addEventListener('pointerleave', stopDrag);

      // Arrow buttons
      const btnLeft = document.querySelector('.arrow.left');
      const btnRight = document.querySelector('.arrow.right');
      const cardWidth = 320; // rough card width + gap
      btnLeft.addEventListener('click', ()=>{
        track.scrollBy({left: -cardWidth, behavior:'smooth'});
      });
      btnRight.addEventListener('click', ()=>{
        track.scrollBy({left: cardWidth, behavior:'smooth'});
      });

      // Keyboard navigation
      window.addEventListener('keydown',(e)=>{
        if(e.key === 'ArrowLeft') track.scrollBy({left:-cardWidth,behavior:'smooth'});
        if(e.key === 'ArrowRight') track.scrollBy({left:cardWidth,behavior:'smooth'});
      });

      // Optional: show/hide fades depending on scroll position
      const fadeLeft = document.querySelector('.fade.left');
      const fadeRight = document.querySelector('.fade.right');
      function updateFades(){
        const maxScroll = track.scrollWidth - track.clientWidth;
        fadeLeft.style.display = track.scrollLeft > 8 ? 'block' : 'none';
        fadeRight.style.display = track.scrollLeft < maxScroll - 8 ? 'block' : 'none';
      }
      track.addEventListener('scroll', updateFades);
      window.addEventListener('resize', updateFades);
      updateFades();

      // Prevent text/image dragging interfering with pointer drag
      track.querySelectorAll('img').forEach(img=>{ img.draggable = false });

      // Accessibility: make cards focusable
      document.querySelectorAll('.card').forEach(card=> card.setAttribute('tabindex','0'));
    })();
  </script>
</body>
</html>




























































































<!doctype html>
<html lang="en">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width,initial-scale=1" />
  <title>Scrollable & Draggable Testimonials</title>
  <style>
    :root{
      --bg:#0f1724;
      --card:#0b1220;
      --accent: #06b6d4;
      --text:#e6eef6;
      --muted:#98a0aa;
    }
    *{box-sizing:border-box}
    body{margin:0;font-family:Inter,ui-sans-serif,system-ui,-apple-system,Segoe UI,Roboto,'Helvetica Neue',Arial;color:var(--text);background:linear-gradient(180deg,#071027 0%, #071122 100%);padding:40px}

    .wrap{max-width:1100px;margin:0 auto}
    h1{font-size:20px;margin:0 0 18px}

    .track-wrap{position:relative}

    .track{
      display:flex;gap:18px;overflow-x:auto;padding:18px;border-radius:12px;scroll-snap-type:x mandatory;-webkit-overflow-scrolling:touch;background:linear-gradient(90deg, rgba(255,255,255,0.02), rgba(255,255,255,0.01));cursor:grab;user-select:none;
      scroll-behavior:smooth; /* smoother scroll */
    }
    .track:active{cursor:grabbing}
    .track::-webkit-scrollbar{height:10px}
    .track::-webkit-scrollbar-thumb{background:rgba(255,255,255,0.06);border-radius:999px}

    .card{min-width:300px;max-width:340px;background:linear-gradient(180deg, rgba(255,255,255,0.02), rgba(255,255,255,0.01));border:1px solid rgba(255,255,255,0.04);padding:18px;border-radius:12px;scroll-snap-align:center;flex-shrink:0;display:flex;flex-direction:column;gap:12px;transition:transform 0.3s ease, box-shadow 0.3s ease}
    .card:hover{transform:translateY(-4px);box-shadow:0 8px 20px rgba(0,0,0,0.25)}

    .meta{display:flex;gap:12px;align-items:center}
    .meta img{width:48px;height:48px;border-radius:50%;object-fit:cover;border:2px solid rgba(255,255,255,0.04)}
    .name{font-weight:600}
    .role{color:var(--muted);font-size:13px}
    .quote{font-size:15px;line-height:1.45;color:var(--text)}

    .fade{position:absolute;top:0;bottom:0;width:56px;pointer-events:none}
    .fade.left{left:0;background:linear-gradient(90deg, rgba(7,17,34,0.95) 0%, rgba(7,17,34,0.00) 100%)}
    .fade.right{right:0;background:linear-gradient(270deg, rgba(7,17,34,0.95) 0%, rgba(7,17,34,0.00) 100%)}

    .arrow{position:absolute;top:50%;transform:translateY(-50%);z-index:6;background:rgba(2,6,23,0.6);border:1px solid rgba(255,255,255,0.04);backdrop-filter:blur(6px);width:40px;height:40px;border-radius:10px;display:flex;align-items:center;justify-content:center;cursor:pointer;transition:background 0.3s ease}
    .arrow:hover{background:rgba(2,6,23,0.8)}
    .arrow svg{width:18px;height:18px;opacity:0.9}
    .arrow.left{left:10px}
    .arrow.right{right:10px}

    @media (max-width:640px){
      .card{min-width:260px}
      .wrap{padding:0 12px}
    }
  </style>
</head>
<body>
  <div class="wrap">
    <h1>What people say</h1>
    <p style="color:var(--muted);margin:6px 0 14px">Drag (hold + move) or swipe testimonials left/right. Click arrows or use keyboard ← → for quick navigation.</p>

    <div class="track-wrap">
      <button class="arrow left" aria-label="scroll left">
        <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M15 18l-6-6 6-6" stroke-linecap="round" stroke-linejoin="round"></path></svg>
      </button>

      <div class="track" id="testimonials">
        <article class="card">
          <div class="meta">
            <img src="https://images.unsplash.com/photo-1544005313-94ddf0286df2?q=80&w=400&auto=format&fit=crop&ixlib=rb-4.0.3&s=1a" alt="Aisha">
            <div>
              <div class="name">Aisha Bello</div>
              <div class="role">Product Designer — Lagos</div>
            </div>
          </div>
          <div class="quote">"This kit levelled up our product visuals in days. The team loved how fast we could iterate and ship."</div>
        </article>

        <article class="card">
          <div class="meta">
            <img src="https://images.unsplash.com/photo-1545996124-1b3a0b5edb5b?q=80&w=400&auto=format&fit=crop&ixlib=rb-4.0.3&s=2b" alt="Chidi">
            <div>
              <div class="name">Chidi Nnaji</div>
              <div class="role">Founder — TechStart</div>
            </div>
          </div>
          <div class="quote">"Customer conversion increased after we added the testimonial section — believable, human, and easy to read."</div>
        </article>

        <article class="card">
          <div class="meta">
            <img src="https://images.unsplash.com/photo-1544725176-7c40e5a2c9f9?q=80&w=400&auto=format&fit=crop&ixlib=rb-4.0.3&s=3c" alt="Maya">
            <div>
              <div class="name">Maya Ade</div>
              <div class="role">Marketing Lead</div>
            </div>
          </div>
          <div class="quote">"Clear layout, easy to navigate on mobile, and the drag interaction is a nice touch for touch-first users."</div>
        </article>

        <article class="card">
          <div class="meta">
            <img src="https://images.unsplash.com/photo-1547425260-76bcadfb4f2c?q=80&w=400&auto=format&fit=crop&ixlib=rb-4.0.3&s=4d" alt="Tunde">
            <div>
              <div class="name">Tunde Okoro</div>
              <div class="role">CTO — FinEdge</div>
            </div>
          </div>
          <div class="quote">"Lightweight and fast — integrates seamlessly into our landing page without blocking other elements."</div>
        </article>

        <article class="card">
          <div class="meta">
            <img src="https://images.unsplash.com/photo-1544006659-f0b21884ce1d?q=80&w=400&auto=format&fit=crop&ixlib=rb-4.0.3&s=5e" alt="Grace">
            <div>
              <div class="name">Grace N.</div>
              <div class="role">Freelance Writer</div>
            </div>
          </div>
          <div class="quote">"I love the card sizing and the smooth scrolling — feels polished and natural on mobile."</div>
        </article>

      </div>

      <button class="arrow right" aria-label="scroll right">
        <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M9 6l6 6-6 6" stroke-linecap="round" stroke-linejoin="round"></path></svg>
      </button>

      <div class="fade left"></div>
      <div class="fade right"></div>
    </div>
  </div>

  <script>
    (function(){
      const track = document.getElementById('testimonials');
      let isDown = false;
      let startX;
      let scrollLeft;
      let velocity = 0;
      let rafId = null;

      track.addEventListener('pointerdown', (e) => {
        isDown = true;
        track.setPointerCapture(e.pointerId);
        startX = e.clientX;
        scrollLeft = track.scrollLeft;
        velocity = 0;
        track.classList.add('dragging');
        e.preventDefault();
      });

      track.addEventListener('pointermove', (e) => {
        if(!isDown) return;
        const x = e.clientX;
        const walk = (startX - x);
        const prev = track.scrollLeft;
        track.scrollLeft = scrollLeft + walk;
        velocity = track.scrollLeft - prev;
      });

      function stopDrag(e){
        if(!isDown) return;
        isDown = false;
        try{ if(e && e.pointerId) track.releasePointerCapture(e.pointerId); }catch(_){}
        track.classList.remove('dragging');
        cancelAnimationFrame(rafId);
        const step = () => {
          if(Math.abs(velocity) < 0.3) return;
          track.scrollLeft += velocity;
          velocity *= 0.93;
          rafId = requestAnimationFrame(step);
        };
        rafId = requestAnimationFrame(step);
      }
      track.addEventListener('pointerup', stopDrag);
      track.addEventListener('pointercancel', stopDrag);
      track.addEventListener('pointerleave', stopDrag);

      const btnLeft = document.querySelector('.arrow.left');
      const btnRight = document.querySelector('.arrow.right');
      const cardWidth = 320;
      btnLeft.addEventListener('click', ()=>{
        track.scrollBy({left: -cardWidth, behavior:'smooth'});
      });
      btnRight.addEventListener('click', ()=>{
        track.scrollBy({left: cardWidth, behavior:'smooth'});
      });

      window.addEventListener('keydown',(e)=>{
        if(e.key === 'ArrowLeft') track.scrollBy({left:-cardWidth,behavior:'smooth'});
        if(e.key === 'ArrowRight') track.scrollBy({left:cardWidth,behavior:'smooth'});
      });

      const fadeLeft = document.querySelector('.fade.left');
      const fadeRight = document.querySelector('.fade.right');
      function updateFades(){
        const maxScroll = track.scrollWidth - track.clientWidth;
        fadeLeft.style.display = track.scrollLeft > 8 ? 'block' : 'none';
        fadeRight.style.display = track.scrollLeft < maxScroll - 8 ? 'block' : 'none';
      }
      track.addEventListener('scroll', updateFades);
      window.addEventListener('resize', updateFades);
      updateFades();

      track.querySelectorAll('img').forEach(img=>{ img.draggable = false });
      document.querySelectorAll('.card').forEach(card=> card.setAttribute('tabindex','0'));
    })();
  </script>
</body>
</html>
